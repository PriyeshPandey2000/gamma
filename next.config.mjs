/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.builder.io'],
    },
    // builder: {
    //     settings: {
    //       dangerouslyAllowSVG: true
    //     }
    //   }
  };
  

export default nextConfig;
